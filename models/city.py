#!/usr/bin/python3
""" City class. Used as base for future cities."""

from models.base_model import BaseModel


class City(BaseModel):
    """ City class. Used as base for future cities."""

    state_id = ''
    name = ''
