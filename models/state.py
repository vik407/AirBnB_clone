#!/usr/bin/python3
""" State class. Used as base for future states."""

from models.base_model import BaseModel


class State(BaseModel):
    """ State class. Used as base for future states."""

    name = ''
